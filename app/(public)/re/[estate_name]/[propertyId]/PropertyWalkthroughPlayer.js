"use client";

/**
 * @deprecated Use `@/components/PropertyVideoGallery` instead.
 * Kept as a thin re-export so existing public-page imports keep working.
 */
export { default } from "@/components/PropertyVideoGallery";
