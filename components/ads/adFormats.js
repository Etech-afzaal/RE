// Format pairs used by the site's ad slots (desktop / phones ≤768px).
// Change a pair here to change every slot that uses it.
// Placement names and positions are listed in docs/ads-network.md.

// Wide banner between page sections.
export const BANNER_FORMATS = { desktop: "leaderboard_728x90", mobile: "mobile_banner_320x100" };

// Large banner at the very top of the home page.
export const BILLBOARD_FORMATS = { desktop: "billboard_970x250", mobile: "mobile_banner_320x100" };

// Listing-style card that sits inside a grid of cards.
export const GRID_CARD_FORMAT = "native_card_400x300";
